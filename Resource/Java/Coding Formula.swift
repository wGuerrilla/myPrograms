// Coding Formula //
// Guide in Code ? JAVA //


// PacMan Project ////
    
    
    
    // Creating a Font // 

// Method that returns an object

Method body for Font object
    accessModifier returnType methodName(parameterType parameterName, parameterType parameterName) {
        return someValue;
    }
private // Access modifier 
returnType // Method will return object
methodName // name
parameterType / Name // file location
parameterType / Name // controls Size using Float,Int,Double etc.
return;

// Create a File object from the file path
--------------
try{
    // code that might fail     Try Block > "tells java to try and run the code inside the block"
} catch (Exception e) {                 // If the code failes got to catch
    // Handle the error
}
------------
File variableName = new File(filePath);
//This creates an object that tells java where to find a file 

File // Class Name > tells java using files from computer
variable Name // name of file
new File(filePath); // Creates a File Object > This stores the location of the font file.
---------------
Font variableName = Font.createFont(Font.TrueT)


array of 7 randomize 10 numbers from 0 to 10
show what is the first card 0 

take lower card onto higher card until you have 1 to 2 pil





